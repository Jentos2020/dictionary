{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run app -seed",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/app",
      "cwd": "${workspaceFolder}",
      "args": ["-seed"]
    },
    {
      "name": "Run app",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/app",
      "cwd": "${workspaceFolder}"
    }
  ]
}